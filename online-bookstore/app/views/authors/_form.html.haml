= form_with(model: author, local: true, class: "form-horizontal") do |form|
  - if author.errors.any?
    #error_explanation
      %h2
        = pluralize(author.errors.count, "error")
        prohibited this author from being saved:
      %ul
        - author.errors.full_messages.each do |message|
          %li= message
  .form-group
    = form.label :Author_name, class: "control-label col-sm-2"
    .col-sm-10
      = form.text_field :Author_name, class: "form-control"
  .actions
    %br
    = form.submit class: "btn btn-default col-sm-2 pull-right"
