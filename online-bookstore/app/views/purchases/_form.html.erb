<%= form_with(model: purchase, local: true) do |form| %>
  <% if purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :User_id %>
    <%= form.text_field :User_id %>
  </div>

  <div class="field">
    <%= form.label :BOOK_ISBN %>
    <%= form.text_field :BOOK_ISBN %>
  </div>

  <div class="field">
    <%= form.label :No_of_copies %>
    <%= form.number_field :No_of_copies %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :date_of_purchase %>
    <%= form.date_select :date_of_purchase %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
